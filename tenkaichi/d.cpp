#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <deque>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <utility>
#include <vector>

#define CK(N, A, B) (A <= N && N < B)
#define REP(i, a, b) for (int i = a; i < b; i++)
#define RREP(i, a, b) for (int i = (b - 1); a <= i; i--)
#define F first
#define S second
#define ll long long

const int INF = 1e9;
const long long LLINF = 1e18;

using namespace std;

/*==================================================*/

/*==================================================*/

//半分前列挙？

int N, K;
long long A[10005];
int B[100005];

long long dp[1000000005];   //dp[i]:価値合計 i のときの、論理和の最小値（論理和は単純増加だからOK）

int main() {
    REP(i,0,N){
        if(){
            
        }
    }
    return 0;
}
